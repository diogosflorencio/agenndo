export const metadata = {
  title: "Operação interna",
  robots: { index: false, follow: false },
};

export default function OperacaoInternaRootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
