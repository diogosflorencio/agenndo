export const metadata = {
  title: "Operações",
  robots: { index: false, follow: false },
};

export default function OperacoesRootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
