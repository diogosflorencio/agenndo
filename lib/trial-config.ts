/** Período de teste gratuito do app (novos negócios). Fonte única para UI e copy. */
export const APP_TRIAL_DAYS = 30;

export function trialDaysLabel(): string {
  return `${APP_TRIAL_DAYS} dias`;
}

export function trialDaysShortLabel(): string {
  return `${APP_TRIAL_DAYS} dias grátis`;
}
